language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip3 install -r requirements.txt
# command to run tests
script:
  - flake8
  - pytest --pyargs ../tf_toolbox
after_success:
  - coveralls
